<?php
    class gStore {
        public static $siteTitle = "GizlySöz";
        public static $nowPage = "home";
    };

    // $page = (isset($_GET['page']) ? $_GET['page'] : 'home');
    // $allPages = ["home","dictionary","crosswords"];
    // if(!in_array($page, $allPages)) {
    //     $page = $allPages[0];
    // }
?>